# Auditoría consolidada — pendientes, errores e incompletitud

Fecha: 2026-08-24

## Metodología

Este documento consolida tres pasadas independientes:

1. **Reconciliación** de los hallazgos abiertos en `AUDITORIA_DECISIONES_Y_PENDIENTES.md`, `REVISION_INDEPENDIENTE_2026-08-14.md`, `RECAPTCHA_ESTADO_Y_PENDIENTES.md` y `GUIA_INTEGRACION_UNIFICADA.md` contra el código real actual (confirmando con `Read`/`Grep`/`git log` si cada hallazgo sigue igual, mejoró o empeoró).
2. **Revisión del código nuevo** agregado en las últimas sesiones: la app `telemetry` completa, el redirector `go_windtv_view`, los cambios de `appConfig.py` y el `.env`.
3. **Barrido fresco** sobre áreas del proyecto que las auditorías previas no habían cubierto todavía (vistas, serializers, middleware, templates, settings, dependencias).

Se excluye deliberadamente todo lo que los documentos ya marcan como "Resuelto" o "decisión de diseño aceptada" salvo que la reconciliación haya encontrado que la situación cambió.

---

## Urgente

| # | Hallazgo | Archivo | Nota |
|---|----------|---------|------|
| 1 | ~~Las rutas nativas `dj_rest_auth.urls` / `dj_rest_auth.registration.urls` / `allauth.urls` evaden reCAPTCHA, la sincronización con PanAccess y el throttle real~~ **RESUELTO (2026-08-25)** -- se recortaron a mano los tres includes, dejando solo login/logout/user/token (dj-rest-auth), los callbacks OAuth de Google/Facebook (allauth), y quitando el registro nativo por completo. Ver `docs/LIMPIEZA_RUTAS_AUTH_NATIVAS_2026-08-25.md`. | `panaccess_wind_integration/urls.py` | Crítico, independiente -- resuelto |
| 2 | ~~Secretos reales versionados en el historial de git: `SECRET_KEY`, `ENCRYPTION_KEY`, `DB_PASSWORD` en claro. Confirmado con `git log --all --full-history -- .env` — 19 commits hasta `bc6b9ff` (2026-07-01, "Dejar de versionar .env"); `git show bc6b9ff^:.env` sigue devolviendo los valores.~~ **RESUELTO (2026-08-26)** -- rotados los 3 valores en producción con `manage.py rotate_secrets` (`SECRET_KEY`/`DB_PASSWORD`) y `manage.py reencrypt_credentials` + `rotate_secrets` (`ENCRYPTION_KEY`, con re-encriptado previo de los 60 registros de `SubscriberLoginInfo.password_hash` que dependían de la llave vieja, para no dejarlos ilegibles). Verificado con `/health/`+`/ready/` en verde y prueba de login real. Ver `docs/ROTACION_SECRETOS_COMPROMETIDOS_2026-08-25.md` para el detalle completo, incluido un incidente breve durante la ejecución (edición manual del `.env` dejó `ENCRYPTION_KEY` vacía por un momento, causando reinicios fallidos en 5 de las 8 instancias Daphne; corregido en minutos). | historial de git / `wind/management/commands/rotate_secrets.py` / `wind/management/commands/reencrypt_credentials.py` | Resuelto -- los 3 valores filtrados en git ya no tienen ningún efecto sobre el sistema actual |

---

## Alto

| # | Hallazgo | Archivo | Nota |
|---|----------|---------|------|
| 3 | `create_subscriber_view` encadena hasta 10 llamadas síncronas a PanAccess dentro de un mismo request. **HERRAMIENTA LISTA (2026-08-26)** -- se agregó un tercer modo `hybrid` (intenta todo sync con un presupuesto de tiempo, corta y delega a background si se agota) además del `sync` (default, sin cambios) y `async` existentes. Ver `docs/APROVISIONAMIENTO_HIBRIDO_SUSCRIPTOR_2026-08-26.md`. Sigue en modo `sync` (default) en producción -- activar `hybrid`/`async` requiere coordinar antes con iOS/Android el caso de respuesta parcial. | `appConfig.py` (`FeatureConfig`), `wind/functions/create_subscriber.py`, `.env` | Puede agotar workers Daphne bajo carga si PanAccess está lento -- mitigación lista, activación pendiente de coordinación con apps |
| 4 | ~~Sin FK real entre `ListOfSubscriber` y sus dependientes: `delete_subscriber_operational_data` borra `SubscriberLoginInfo`/`SubscriberEmailRegistry`/`SubscriberDocumentRegistry`/`SubscriberInfo` pero no toca `UDIDAuthRequest`/`DeviceSession`; tampoco corre dentro de `transaction.atomic()`.~~ **RESUELTO (2026-08-26)** -- el cierre manual ya revocaba dispositivos (no era el riesgo real); el hueco genuino estaba en el borrado automático por sincronización (`_delete_local_subscribers_not_in_remote`), que ahora también revoca `UDIDAuthRequest`/`DeviceSession` antes de borrar, además de `transaction.atomic()` en el borrado y un correo de aviso nuevo ("cuenta cerrada") en ambos caminos. `EncryptedCredentialsLog`/`AuthAuditLog` son logs de auditoría, correcto que sobrevivan -- esa parte del hallazgo original era imprecisa. Ver `docs/CIERRE_CUENTA_REVOCACION_SYNC_2026-08-26.md`. Verificado end-to-end contra producción con un suscriptor sintético de prueba. | `wind/functions/getSubscriber.py`, `wind/services/account_closed_email.py`, `wind/tasks.py` | Dispositivos huérfanos ya no pueden seguir autenticando tras el borrado -- verificado en producción |
| 5 | ~~Login (social y manual) sin throttle específico anti fuerza-bruta — solo el anónimo genérico (`AnonBurstThrottle`). Amplifica llamadas a PanAccess bajo un ataque de credential-stuffing.~~ **RESUELTO (2026-08-26)** -- `LoginThrottle` propio (`10/minute` por defecto) en `/api/auth/login/` (el social ya tenía `SocialLoginThrottle` desde antes), más caché de "no encontrado" en `_discover_login_by_login1` (Medio #14) y bloqueo temporal de cuenta tras intentos fallidos (cache-based, no vía `SubscriberInfo` -- ver nota en el documento). Aprovechado además para diagnosticar y mitigar la demora de login (10-15s) reportada por el cliente (índices funcionales faltantes + resolución duplicada de `subscriber_code`). Ver `docs/OPTIMIZACION_DESCUBRIMIENTO_BLOQUEO_LOGIN_2026-08-26.md` y `docs/OPTIMIZACION_LATENCIA_LOGIN_2026-08-26.md`. | `wind/throttles.py`, `wind/services/subscriber_auth.py`, `appConfig.py` | Verificado localmente (py_compile/manage.py check); falta medir en vivo contra producción |
| 6 | ~~El cambio de password nativo de `dj-rest-auth` no exige `old_password` ni invalida sesiones existentes tras el cambio — mismo problema de fondo que el #1, aplicado a esa vista puntual.~~ **RESUELTO -- FASE 1 Y FASE 2 COMPLETAS (2026-08-28)** -- la vista nativa de dj-rest-auth ni está montada (no aplica); la invalidación de sesiones ya estaba resuelta desde Alto #4 (`mark_password_changed`/`revoke_all_device_sessions_for_subscriber`, re-confirmada en pruebas). El gap real era que `POST /api/v1/profile/password/` no pedía la contraseña actual. Fase 1 (2026-08-26) agregó `oldPass` opcional, verificado con `verify_panaccess_credentials` + bloqueo temporal tras intentos fallidos; probados los 5 casos contra producción con una cuenta real (correcta/incorrecta/ausente/bloqueo/liberación). Fase 2 (2026-08-28), confirmado con el cliente que Web/Android/iOS ya validan/envían la contraseña actual: `oldPass` pasa a **obligatorio** en el serializer, eliminado el branch que permitía omitirlo -- cierra el hueco real (un JWT robado ya no alcanza por sí solo, sin importar si el cliente valida "localmente" o no, porque el backend ahora lo exige siempre). Ver `docs/VERIFICACION_CONTRASENA_ACTUAL_2026-08-26.md` (fase 1), `docs/VERIFICACION_CONTRASENA_ACTUAL_FASE2_2026-08-28.md` (fase 2) y `docs/PLAN_VERIFICACION_CONTRASENA_ACTUAL_2026-08-26.md` (diseño original). | `wind/api/profile/views.py`, `wind/api/profile/serializers.py`, `appConfig.py`, `wind/templates/wind/dashboard.html` | Resuelto y verificado en producción, incluido el caso "sin `oldPass`" → 400. De paso se detectó un problema aparte de cadena de certificados SSL en `backend.wind.do` (ver Medio #41), sin relación con este hallazgo |
| 7 | reCAPTCHA totalmente inactivo en los 4 flujos que lo soportan (registro, olvidé contraseña, restablecer contraseña, eliminar cuenta): `RECAPTCHA_SECRET_KEY` vacío en `.env`, y además falta agregar el widget en `register.html`, `forgot-password.html`, `reset-password.html` y el modal de "eliminar cuenta". **PLAN Y GUÍA LISTOS (2026-08-26)**, sin implementar todavía -- de paso se detectó que Google migró todo reCAPTCHA a "Google Cloud Fraud Defense" (ya no se pueden crear llaves "Classic" sueltas desde Q3 2024, migración automática completa desde Q1 2026), lo que agrega un paso nuevo para conseguir la secret key, aunque el código actual (`wind/utils/recaptcha.py`) sigue funcionando sin cambios. Ver `docs/PLAN_ACTIVACION_RECAPTCHA_2026-08-26.md` (qué falta y en qué orden) y `docs/GUIA_CREAR_LLAVES_RECAPTCHA_2026-08-26.md` (cómo conseguir las llaves, paso a paso). | `.env`, templates de `wind/templates/wind/` | Backend ya soporta reCAPTCHA; conseguir las llaves probablemente requiere al cliente (cuenta de Google propia) |

---

## Medio

| # | Hallazgo | Archivo | Nota |
|---|----------|---------|------|
| 8 | `crypto_tv.py`: el esquema legado (`hybrid_encrypt_for_app`, el que usan hoy las marcas `bromteck`/`cableatlantico` en `appVideo`, no `wind`) sigue en AES-CBC sin HMAC/AEAD. El esquema nuevo (`hybrid_encrypt_for_device_public_key`, ya wireado en `appVideo` como `generateEphemeralKeyPair()`) ya usa AES-GCM — bajó de Alto a Medio porque el riesgo real hoy es solo el camino legado, y no afecta a Wind (`udid.enabled: false` en su config de marca, no usa este flujo). **Mitigación preventiva (2026-08-28):** se agregó `tempTokenRequired: true` al bloque `wind` en `appVideo/src/config/brands.js` -- cambio inerte mientras `udid.enabled` siga en `false` (confirmado: `useUdidLoginFlow.js` corta antes de leer ese flag), pero asegura que si algún día se habilita el pareo UDID para Wind, arranque directo con el esquema moderno en vez de con el legado. | `wind/utils/crypto_tv.py`, `appVideo/src/config/brands.js` | Migrar `bromteck`/`cableatlantico` cuando/si se decide -- fuera del alcance de la auditoría de Wind, depende del cliente que administra esas marcas |
| 9 | ~~XSS vía `innerHTML` sin escapar en una página de debug: `subscriber_test.html` inserta campos del suscriptor (`comment`, `address`, `firstName`, etc.) directamente en el DOM sin `escapeHtml()`/`textContent`, a diferencia de `dashboard.html` que sí lo hace bien.~~ **RESUELTO (2026-08-28)** -- se agregó la misma función `escapeHtml()` que ya usa `dashboard.html` y se aplicó a `renderValue(val)` antes de insertarlo en el DOM (`renderFields()`). Verificado con `manage.py check`. | `wind/templates/wind/subscriber_test.html` | Resuelto |
| 10 | ~~Filas de telemetría OTT parcialmente corruptas (p. ej. `actionId=8` mal formado) se descartan en silencio, sin `logger.warning`.~~ **RESUELTO (2026-08-28)** -- ambos puntos de descarte silenciosa ahora loguean `logger.warning` y se cuentan en `result["malformed_skipped"]`. Ver `docs/TELEMETRIA_OTT_LOGGING_Y_CONSTRAINTS_2026-08-28.md`. | `telemetry/services/panaccess_ott_ingest.py` | Resuelto |
| 11 | ~~`bulk_create(..., ignore_conflicts=True)` en la ingesta de telemetría silencia cualquier violación de constraint, no solo el duplicado esperado por `record_id`. Hoy no hay FK adicional, así que el riesgo es bajo — pero si se agrega una en el futuro, este patrón esconderá pérdida de datos real sin log diferenciado.~~ **RESUELTO (2026-08-28)** -- se agregó `unique_fields=["record_id"]` a la llamada, acotando el `ON CONFLICT DO NOTHING` a esa columna específica. | `telemetry/services/panaccess_ott_ingest.py` | Resuelto |
| 12 | ~~`GET /api/v1/telemetry/top-channels/` solo exige `IsAuthenticated`, sin distinción de rol — no es sensible hoy (ranking global igual para todos), pero no hay barrera si en el futuro se agrega data más granular al mismo endpoint.~~ **RESUELTO (2026-08-28), guardarraíl preventivo** -- sin cambio de comportamiento (correcto hoy); se agregó un comentario explícito en `TopChannelsGlobalView` para que quien agregue datos más granulares en el futuro no se olvide de sumar autorización. | `telemetry/views.py:24` | Resuelto (preventivo) |
| 13 | ~~El reset/cambio de contraseña no invalida proactivamente los JWT ya emitidos — `SIMPLE_JWT` solo blacklistea un refresh token después de rotarlo. Un access/refresh token robado antes del cambio sigue funcionando hasta que expira por su cuenta.~~ **RESUELTO** -- confirmado en `wind/services/jwt_invalidation.py`: `PasswordAwareJWTAuthentication` rechaza cualquier access token con `iat` anterior al último cambio de contraseña (`UserSecurityProfile.password_changed_at`), y `mark_password_changed()` blacklistea de una vez todos los refresh tokens vigentes del usuario (no solo el rotado). Se dispara desde `sync_password_locally()`, compartida por los 3 flujos (perfil, reset, legacy). Ya verificado en vivo durante las pruebas de Alto #6 en producción. No se encontró en qué sesión se implementó -- se corrige la tabla (2026-08-28). | `wind/services/jwt_invalidation.py` | Resuelto, sin acción pendiente |
| 14 | ~~`_discover_login_by_login1` puede disparar hasta `PANACCESS_LOGIN_DISCOVERY_MAX_CALLS` (40 por defecto) llamadas síncronas a PanAccess paginando el catálogo completo dentro de un mismo request, si el `login1` numérico no se encuentra localmente. Acotado, pero es una amplificación real solo protegida por el throttle anónimo genérico.~~ **RESUELTO (2026-08-26)**, junto con Alto #5 -- caché de "no encontrado" (Redis, TTL corto) evita repetir el barrido completo en reintentos contra el mismo `login1`, más el throttle dedicado. Ver `docs/OPTIMIZACION_DESCUBRIMIENTO_BLOQUEO_LOGIN_2026-08-26.md`. | `wind/services/subscriber_auth.py` | — |
| 15 | ~~WebSockets sin `AllowedHostsOriginValidator`.~~ **RESUELTO (2026-08-28)** -- no se usó el validador de Channels tal cual (rechaza cualquier conexión sin header `Origin`, lo que hubiera cortado clientes nativos sin avisar). Se agregó `wind/utils/ws_origin_validator.py` (`NativeAwareOriginValidator`): permite conexiones sin `Origin` (apps nativas), y si viene `Origin`, lo exige contra `settings.CORS_ALLOWED_ORIGINS` (la misma lista que ya autoriza CORS para la API REST). Envuelve todo `websocket_urlpatterns` en `asgi.py`. Verificado funcionalmente: sin Origin → permite; Origin de la lista → permite; Origin ajeno → rechaza. | `wind/utils/ws_origin_validator.py`, `panaccess_wind_integration/asgi.py` | Resuelto |
| 16 | Fingerprint de dispositivo evadible rotando los headers que lo derivan, con comportamiento fail-open. Limitación estructural aceptada, no una decisión nueva. | — | Revisado (2026-08-28): sigue sin una solución de bajo riesgo -- el fingerprint solo alimenta rate-limiting, no es la barrera de acceso real (esa es JWT/device_token/temp_token). Sin acción. |
| 17 | ~~Faltan índices funcionales en columnas de búsqueda frecuente: `SubscriberEmailRegistry.email`, `SubscriberLoginInfo.login2`, `ListOfSubscriber.code`.~~ **RESUELTO (2026-08-26)** -- migración `0009_login_functional_indexes.py` agrega los 3 índices `Upper(...)` faltantes (el de `ListOfSubscriber.emails` ya existía desde la migración `0004`). Parte del diagnóstico de la demora de login reportada por el cliente. Ver `docs/OPTIMIZACION_LATENCIA_LOGIN_2026-08-26.md`. | modelos respectivos, `wind/migrations/0009_login_functional_indexes.py` | Verificado localmente; falta aplicar `migrate` en producción y medir |
| 18 | ~~`is_reset_token_used`/`mark_reset_token_used` dependen solo de Redis; si Redis falla, hacen fail-open (tratan el token como no usado) — un enlace de reset filtrado se podría reutilizar durante una caída de Redis.~~ **RESUELTO** -- confirmado en el código actual (`wind/services/password_reset.py:74-110`): `PasswordResetTokenUse` (modelo de BD) ya es la fuente de verdad, Redis quedó solo como caché best-effort; una caída de Redis ya no permite reutilizar un token usado. No se encontró en qué sesión se implementó -- se corrige la tabla (2026-08-28). | `wind/services/password_reset.py:74-110` | Resuelto, sin acción pendiente |
| 19 | ~~`pre_social_login` confía en el email que devuelve el proveedor social y lo trata como verificado, sin revisar el flag `email_verified` del proveedor, antes de fusionarlo con una cuenta local existente.~~ **RESUELTO** -- confirmado en el código actual (`wind/adapters.py:53-97`): `_is_email_verified_by_provider()` revisa `sociallogin.email_addresses`/`extra_data.email_verified` y `pre_social_login` rechaza con `ValidationError` si no viene verificado, antes de fusionar con una cuenta local existente. No se encontró en qué sesión se implementó, pero ya está en producción -- se corrige la tabla (2026-08-28). | `wind/adapters.py:53-97` | Resuelto, sin acción pendiente |
| 41 | Cadena de certificado SSL incompleta en `backend.wind.do`: `/etc/nginx/cdn1.wind.do.crt` (el archivo activo, referenciado desde `sites-enabled/panaccess-wind.conf`) tiene el certificado hoja (`*.wind.do`, GoDaddy) **pegado dos veces** en vez de hoja + intermedio (`Go Daddy Secure Certificate Authority - G2`). Detectado al depurar por qué `curl` fallaba con "unable to get local issuer certificate" contra endpoints propios durante las pruebas de Alto #6. Navegadores no lo notan (cachean/auto-completan el intermedio), pero clientes con validación TLS estricta (`curl` sin `-k`, algunos stacks TLS de apps móviles) pueden rechazar la conexión. El intermedio correcto ya existe en el servidor, sin usar, en `/etc/nginx/gd-g2_iis_intermediates.p7b` (formato PKCS#7, hay que convertirlo a PEM). **Diagnosticado pero NO corregido a pedido del cliente** -- la configuración de nginx/SSL la administra el cliente directamente y prefiere no delegar ese cambio para no arriesgar otra cosa que tenga configurada ahí sin que se sepa. Queda a la espera de que el cliente lo aplique por su cuenta, o autorice explícitamente hacerlo. | `/etc/nginx/cdn1.wind.do.crt`, `/etc/nginx/sites-available/panaccess-wind.conf` (fuera del repo de este proyecto) | No tocar sin autorización explícita del cliente |

---

## Bajo

| # | Hallazgo | Archivo | Nota |
|---|----------|---------|------|
| 20 | Páginas de test/debug (`subscriber-test/`, `login-test/`, `login-test-facebook/`) registradas incondicionalmente en `urls.py`, sin gate de `settings.DEBUG` — quedan accesibles en producción. | `wind/urls.py:45,54,57` | Una de ellas es la del hallazgo #9 de arriba |
| 21 | `dashboard_view` no tiene `login_required` server-side (es intencional: la protección real vive en los endpoints `/api/v1/...` vía JWT) — no es un problema hoy, pero se perdería si algún día se agrega data server-side al contexto de la plantilla. | `wind/views.py:787-789` | Informativo, vigilar a futuro |
| 22 | `event_date` de un evento de telemetría cae en la fecha del servidor si el timestamp original es inválido, en vez de descartarse — puede distorsionar levemente el ranking en días con datos sucios. | `telemetry/services/panaccess_ott_ingest.py:240` | Impacto menor dado el volumen esperado |
| 23 | Race teórica entre cierre de cuenta y sync periódico para un suscriptor que nunca se sincronizó antes — ventana de pocos segundos, ya documentada, no crítica. | `wind/services/subscriber_closure.py` | — |
| 24 | Code smells menores: `attempts_count` de UDID sin usar, orden lexicográfico en `subscriber_code_generator.py`, `client_ip` en WS consumers siempre `127.0.0.1`. | varios | — |
| 25 | Código muerto: subsistema de rate-limit por WebSocket abandonado, 8 serializers sin uso, imports muertos. | varios | Candidato a limpieza |
| 26 | Huecos de cobertura de test en la integración PanAccess, login social, invalidación de JWT y device session. | `wind/tests/` | — |
| 27 | `requirements.txt` está en UTF-16 en vez de UTF-8 (todas las dependencias sí están fijadas con `==`, no es un problema de reproducibilidad) — puede romper linters/herramientas que asuman UTF-8. | `requirements.txt` | Resguardar en UTF-8 |
| 28 | ~~`DeviceSession` no tiene expiración ni limpieza automática — sin decisión tomada todavía.~~ **RESUELTO (2026-08-28)** -- tarea periódica `expire_idle_device_sessions_task` (Celery beat, corre a diario) revoca automáticamente toda sesión sin actividad (`last_seen_at`) hace más de 183 días (`DEVICE_SESSION_IDLE_EXPIRY_DAYS`, ajustable por `.env`). `device_consumers.py` ya rechazaba `status != "active"`, así que no hizo falta tocar el consumer. Ver `docs/EXPIRACION_DEVICE_SESSION_INACTIVA_2026-08-28.md`. | `appConfig.py` (`CeleryConfig`), `wind/tasks.py`, `panaccess_wind_integration/settings.py` | Verificado localmente (py_compile/manage.py check); falta confirmar que Celery Beat tomó el schedule nuevo en producción |
| 29 | El brand `bromteck` en appVideo sigue apuntando a `http://` en vez de `https://`. | `appVideo/src/config/brands.js` | Pendiente del equipo de appVideo |
| 30 | Router de réplica de base de datos sin failover; falta un timeout de conexión a DB explícito. | `DatabaseConfig` | — |

---

## Posibles mejoras

| # | Mejora | Nota |
|---|--------|------|
| 31 | Definir el site-key de reCAPTCHA para mobile (iOS/Android) en los flujos de "olvidé contraseña" / "eliminar cuenta". | Bloqueante para que el punto 7 se cierre del lado mobile |
| 32 | Desplegar el `.mmdb` de GeoIP en el servidor real de producción (`GEOIP_CITY_DB_PATH`). | — |
| 33 | ~~appVideo: `splashAuthFlow.js` no reabre `ws/device/` tras un refresh de sesión; `loginFlow.js` no espera a que el `device_token` quede persistido antes de continuar.~~ **REVISADO (2026-08-28), no queda acción pendiente.** El primer punto ya está resuelto -- no en `splashAuthFlow.js` puntual, sino con un watchdog centralizado (`ensureDeviceSessionConnected()` en `appVideo/src/hooks/useAppLifecycle.js:40-56`) que garantiza la conexión a `/ws/device/` sin importar qué camino de login/reactivación se tomó, con comentario propio citando este mismo hallazgo. El segundo es diseño intencional, no un bug: `loginFlow.js` es fire-and-forget a propósito (comentario explícito: "nunca debe bloquear ni poder tumbar el login principal contra PanAccess") y el único consumidor del `device_token` (`LinkedDevicesPanel.jsx`) ya se actualiza vía callback (`setOnDeviceRegistered`) en vez de asumir que está listo de entrada -- patrón agregado específicamente para evitar la condición de carrera. | `appVideo/src/hooks/useAppLifecycle.js`, `appVideo/src/services/loginFlow.js`, `appVideo/src/services/deviceSessionService.js` | Sin acción pendiente |
| 34 | Estandarizar el formato de QR/TV para el emparejamiento de dispositivos. | — |
| 35 | Documentar (o quitar) el campo `name` del JSON de `top-channels`, que hoy no se usa del lado de appVideo (`buildMostWatchedItems` solo cruza por `channel_id`). | No es un bug, solo peso muerto en el contrato |
| 36 | Construir el health endpoint para Zabbix/PRTG que se propuso en la sesión de monitoreo. | — |
| 37 | Dar de alta el worker de systemd para la cola `telemetry` en el servidor de producción (ingest + aggregate no corren sin esto). | Bloqueante para que la app `telemetry` funcione en producción |
| 38 | Completar la Fase 4 de cierre de cuenta (endpoint HTTP admin/autoservicio). | — |
| 39 | Confirmar y corregir el mismatch de paquete Android: `WIND_APP_GOOGLE_PLAY_URL` usa `com.wind.windtv`, mientras que `WINDTV_ANDROID_PACKAGE` (el real, confirmado por el equipo Android) es `com.wind.android.streaming`. | — |
| 40 | Evaluar migrar el esquema legado de `crypto_tv.py` a AEAD cuando el cliente confirme que las apps pueden actualizar el lado que descifra. | Relacionado con el hallazgo #8 |

---

## Resumen numérico

| Categoría | Cantidad |
|-----------|----------|
| Urgente | 2 |
| Alto | 5 |
| Medio | 13 |
| Bajo | 11 |
| Posibles mejoras | 10 |

**Los dos únicos "Urgente" no eran hallazgos nuevos** — eran el mismo problema (rutas nativas de auth) y el mismo riesgo (secretos en git) que ya estaban señalados en `REVISION_INDEPENDIENTE_2026-08-14.md`. El primero (rutas nativas) se resolvió el 2026-08-25 (ver `docs/LIMPIEZA_RUTAS_AUTH_NATIVAS_2026-08-25.md`). El segundo (secretos en el historial de git) se resolvió el 2026-08-26 -- los 3 valores (`SECRET_KEY`, `ENCRYPTION_KEY`, `DB_PASSWORD`) ya están rotados en producción, con los 60 registros afectados por `ENCRYPTION_KEY` re-encriptados primero para no perder datos. Ver `docs/ROTACION_SECRETOS_COMPROMETIDOS_2026-08-25.md`.
